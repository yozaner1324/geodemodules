plugins {
    id 'java'
}

group 'org.apache.geode'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile(project(':module-service'))

    compile(project(':sub-module1'))
}
